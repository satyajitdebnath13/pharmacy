import { DemandFilterPipe } from './demand-filter.pipe';

describe('DemandFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DemandFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
